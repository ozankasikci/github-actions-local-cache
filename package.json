{
  "name": "github-actions-local-cache",
  "version": "1.0.0",
  "description": "A GitHub Action for local caching on runners",
  "main": "dist/main.js",
  "scripts": {
    "build": "ncc build src/main.js -o dist --source-map --license licenses.txt",
    "package": "npm run build",
    "test": "jest",
    "all": "npm run build && npm run package && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourusername/github-actions-local-cache.git"
  },
  "keywords": [
    "actions",
    "node",
    "cache",
    "github"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/cache": "^3.2.4",
    "@actions/glob": "^0.4.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0"
  }
}